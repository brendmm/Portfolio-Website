class Image {
     width = 'width'
     length = 'length'
     src = 'src'
}

function ImageGridOptimization (gridWidth,gridLength) {
    //  gridWidth and gridLength are equal to the overall WxL we are trying to fit into
    let gridRatio = gridLength/gridLength
}